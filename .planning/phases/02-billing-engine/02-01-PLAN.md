---
phase: 02-billing-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/utils/aging.ts
  - src/lib/utils/validation.ts
  - src/lib/utils/invoice-number.ts
  - src/components/layout/sidebar-client.tsx
  - src/components/invoices/status-badge.tsx
autonomous: true
requirements: [INV-09, COLL-01]

must_haves:
  truths:
    - "Aging bucket utility correctly categorizes overdue invoices into current/30day/60day/90plus buckets"
    - "Schema has customerNotes table, isCollectionsFlagged and lastReminderDate on customers, and extended collectionNotes fields"
    - "Sidebar shows Recurring and Collections nav items for staff users"
    - "Invoice status badge renders correct color-coded variants for all 6 statuses"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "customerNotes table, amended customers columns, amended collectionNotes columns"
      contains: "customerNotes"
    - path: "src/lib/utils/aging.ts"
      provides: "getAgingBucket, agingColors, agingLabels utilities"
      exports: ["getAgingBucket", "agingColors", "agingLabels"]
    - path: "src/lib/utils/validation.ts"
      provides: "Zod schemas for all Phase 2 forms"
      exports: ["createCustomerSchema", "createInvoiceSchema", "lineItemSchema", "recordPaymentSchema"]
    - path: "src/lib/utils/invoice-number.ts"
      provides: "Invoice number generation"
      exports: ["getNextInvoiceNumber"]
    - path: "src/components/invoices/status-badge.tsx"
      provides: "Color-coded status badge component"
      exports: ["InvoiceStatusBadge"]
  key_links:
    - from: "src/lib/utils/aging.ts"
      to: "date-fns"
      via: "differenceInDays import"
      pattern: "differenceInDays"
    - from: "src/lib/utils/validation.ts"
      to: "zod"
      via: "z.object schema definitions"
      pattern: "z\\.object"
---

<objective>
Prepare the foundation for all Phase 2 billing features: amend database schema for missing fields, install missing shadcn components, create shared utility functions, and update sidebar navigation.

Purpose: Every subsequent plan depends on these schema changes, utilities, and UI primitives.
Output: Amended schema, aging/validation/invoice-number utilities, status badge component, updated sidebar nav, installed shadcn components.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-billing-engine/02-CONTEXT.md
@.planning/phases/02-billing-engine/02-RESEARCH.md

<interfaces>
<!-- Existing schema structure (to be amended) -->
From src/lib/db/schema.ts:
```typescript
export const customers = pgTable("customers", { ... }); // needs isCollectionsFlagged, lastReminderDate
export const collectionNotes = pgTable("collection_notes", { ... }); // needs customerId, noteType, promisedDate, promisedAmountCents; invoiceId becomes optional
// Missing: customerNotes table
```

From src/lib/formatting.ts:
```typescript
export function formatCurrency(cents: number): string;
export function formatCurrencyParts(cents: number): { dollars: string; sign: string };
```

From src/components/layout/sidebar-client.tsx:
```typescript
const allNavItems: NavItem[] = [
  { label: "Dashboard", href: "/dashboard", icon: ..., group: "main" },
  { label: "Invoices", href: "/invoices", icon: ..., group: "main" },
  { label: "Payments", href: "/payments", icon: ..., group: "main" },
  { label: "Customers", href: "/customers", icon: ..., group: "main" },
  { label: "Reports", href: "/reports", icon: ..., group: "reports" },
  { label: "Settings", href: "/settings", icon: ..., group: "settings" },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema amendments, shadcn install, and sidebar nav update</name>
  <files>src/lib/db/schema.ts, src/components/layout/sidebar-client.tsx</files>
  <read_first>src/lib/db/schema.ts, src/components/layout/sidebar-client.tsx, .planning/phases/02-billing-engine/02-RESEARCH.md</read_first>
  <action>
**1. Install missing shadcn components:**
```bash
npx shadcn@latest add dialog select tabs textarea switch popover calendar progress
```

**2. Amend `src/lib/db/schema.ts`:**

Add a new `customerNotes` table AFTER the `customers` table:
```typescript
export const customerNotes = pgTable("customer_notes", {
  id: uuid("id").defaultRandom().primaryKey(),
  customerId: uuid("customer_id").references(() => customers.id, { onDelete: "cascade" }).notNull(),
  note: text("note").notNull(),
  createdBy: uuid("created_by").references(() => users.id),
  createdAt: timestamp("created_at").defaultNow().notNull(),
});
```

Add two columns to the `customers` table:
```typescript
isCollectionsFlagged: boolean("is_collections_flagged").default(false).notNull(),
lastReminderDate: date("last_reminder_date"),
```

Amend `collectionNotes` table to:
- Add `customerId: uuid("customer_id").references(() => customers.id).notNull()`
- Change `invoiceId` to optional (remove `.notNull()`)
- Add `noteType: text("note_type").default("note").notNull()` (values: "note" | "promise_to_pay")
- Add `promisedDate: date("promised_date")`
- Add `promisedAmountCents: integer("promised_amount_cents")`

**3. Push schema to database:**
```bash
npx drizzle-kit push
```

**4. Update sidebar navigation in `src/components/layout/sidebar-client.tsx`:**

Add two imports at the top: `RefreshCw, AlertTriangle` from lucide-react.

Add two entries to `allNavItems` array AFTER the "Customers" entry and BEFORE "Reports":
```typescript
{ label: "Recurring", href: "/recurring", icon: <RefreshCw size={20} strokeWidth={1.5} />, group: "main" },
{ label: "Collections", href: "/collections", icon: <AlertTriangle size={20} strokeWidth={1.5} />, group: "main" },
```
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/billinghub && grep -c "customerNotes" src/lib/db/schema.ts && grep -c "isCollectionsFlagged" src/lib/db/schema.ts && grep -c "RefreshCw" src/components/layout/sidebar-client.tsx && ls src/components/ui/dialog.tsx src/components/ui/select.tsx src/components/ui/tabs.tsx src/components/ui/calendar.tsx src/components/ui/progress.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db/schema.ts contains `export const customerNotes = pgTable("customer_notes"`
    - src/lib/db/schema.ts contains `isCollectionsFlagged: boolean("is_collections_flagged")`
    - src/lib/db/schema.ts contains `lastReminderDate: date("last_reminder_date")`
    - src/lib/db/schema.ts collectionNotes table contains `customerId: uuid("customer_id")`
    - src/lib/db/schema.ts collectionNotes table contains `noteType: text("note_type")`
    - src/lib/db/schema.ts collectionNotes table contains `promisedDate: date("promised_date")`
    - src/lib/db/schema.ts collectionNotes table contains `promisedAmountCents: integer("promised_amount_cents")`
    - src/components/layout/sidebar-client.tsx contains `RefreshCw` and `AlertTriangle` imports
    - src/components/layout/sidebar-client.tsx contains `{ label: "Recurring"`
    - src/components/layout/sidebar-client.tsx contains `{ label: "Collections"`
    - src/components/ui/dialog.tsx exists
    - src/components/ui/select.tsx exists
    - src/components/ui/tabs.tsx exists
    - src/components/ui/calendar.tsx exists
    - src/components/ui/progress.tsx exists
  </acceptance_criteria>
  <done>Schema has all Phase 2 tables and columns, shadcn components installed, sidebar shows 6 main nav items including Recurring and Collections</done>
</task>

<task type="auto">
  <name>Task 2: Shared utilities - aging, validation schemas, invoice number, status badge</name>
  <files>src/lib/utils/aging.ts, src/lib/utils/validation.ts, src/lib/utils/invoice-number.ts, src/components/invoices/status-badge.tsx</files>
  <read_first>src/lib/db/schema.ts, src/lib/formatting.ts, .planning/phases/02-billing-engine/02-RESEARCH.md</read_first>
  <action>
**1. Create `src/lib/utils/aging.ts`:**
```typescript
import { differenceInDays } from "date-fns";

export type AgingBucket = "current" | "30day" | "60day" | "90plus";

export function getAgingBucket(dueDate: string | Date, asOf: Date = new Date()): AgingBucket | null {
  const due = typeof dueDate === "string" ? new Date(dueDate) : dueDate;
  const daysOverdue = differenceInDays(asOf, due);
  if (daysOverdue <= 0) return null; // not overdue
  if (daysOverdue <= 30) return "current";
  if (daysOverdue <= 60) return "30day";
  if (daysOverdue <= 90) return "60day";
  return "90plus";
}

export const agingColors: Record<AgingBucket, string> = {
  current: "text-green-600",
  "30day": "text-yellow-600",
  "60day": "text-orange-500",
  "90plus": "text-red-600",
};

export const agingBadgeColors: Record<AgingBucket, string> = {
  current: "bg-green-100 text-green-700 border-green-200",
  "30day": "bg-amber-100 text-amber-700 border-amber-200",
  "60day": "bg-orange-100 text-orange-700 border-orange-200",
  "90plus": "bg-red-100 text-red-700 border-red-200",
};

export const agingLabels: Record<AgingBucket, string> = {
  current: "Current (1-30 days)",
  "30day": "30 Day (31-60)",
  "60day": "60 Day (61-90)",
  "90plus": "90+ Days",
};
```

**2. Create `src/lib/utils/validation.ts`:**
```typescript
import { z } from "zod";

export const createCustomerSchema = z.object({
  name: z.string().min(1, "Name is required").max(100),
  email: z.string().email("Invalid email").optional().or(z.literal("")),
  phone: z.string().max(20).optional().or(z.literal("")),
  address: z.string().max(500).optional().or(z.literal("")),
  vesselName: z.string().max(100).optional().or(z.literal("")),
  vesselType: z.enum(["sailboat", "powerboat", "catamaran"]).optional(),
  vesselLength: z.number().int().min(1).max(200).optional().nullable(),
  dock: z.string().max(1).optional().or(z.literal("")),
  slipNumber: z.string().max(10).optional().or(z.literal("")),
  notes: z.string().max(1000).optional().or(z.literal("")),
});

export const lineItemSchema = z.object({
  category: z.enum(["slip_rental", "fuel", "maintenance", "amenity", "service", "other"]),
  description: z.string().min(1, "Description is required"),
  quantity: z.number().int().min(1, "Quantity must be at least 1"),
  unitPriceCents: z.number().int().min(0, "Price must be non-negative"),
});

export const createInvoiceSchema = z.object({
  customerId: z.string().uuid("Select a customer"),
  issueDate: z.string().min(1, "Issue date is required"),
  dueDate: z.string().min(1, "Due date is required"),
  taxRate: z.number().min(0).max(1),
  notes: z.string().optional().or(z.literal("")),
  items: z.array(lineItemSchema).min(1, "At least one line item is required"),
});

export const recordPaymentSchema = z.object({
  invoiceId: z.string().uuid(),
  amountCents: z.number().int().min(1, "Amount must be greater than 0"),
  method: z.enum(["cash", "check", "credit_card", "bank_transfer"]),
  reference: z.string().optional().or(z.literal("")),
  paymentDate: z.string().min(1, "Payment date is required"),
  notes: z.string().optional().or(z.literal("")),
});

export const createRecurringTemplateSchema = z.object({
  customerId: z.string().uuid("Select a customer"),
  name: z.string().min(1, "Template name is required").max(100),
  frequency: z.enum(["monthly", "quarterly", "annual"]),
  nextInvoiceDate: z.string().min(1, "Next invoice date is required"),
  items: z.array(lineItemSchema).min(1, "At least one line item is required"),
});

export const collectionNoteSchema = z.object({
  customerId: z.string().uuid(),
  noteType: z.enum(["note", "promise_to_pay"]),
  note: z.string().min(1, "Note is required"),
  promisedDate: z.string().optional().or(z.literal("")),
  promisedAmountCents: z.number().int().min(0).optional().nullable(),
});

export type CreateCustomerInput = z.infer<typeof createCustomerSchema>;
export type CreateInvoiceInput = z.infer<typeof createInvoiceSchema>;
export type RecordPaymentInput = z.infer<typeof recordPaymentSchema>;
export type CreateRecurringTemplateInput = z.infer<typeof createRecurringTemplateSchema>;
export type CollectionNoteInput = z.infer<typeof collectionNoteSchema>;
```

**3. Create `src/lib/utils/invoice-number.ts`:**
```typescript
import { db } from "@/lib/db";
import { invoices } from "@/lib/db/schema";
import { sql } from "drizzle-orm";

export async function getNextInvoiceNumber(): Promise<string> {
  const result = await db
    .select({ maxNum: sql<string>`MAX(${invoices.invoiceNumber})` })
    .from(invoices);
  const current = result[0]?.maxNum;
  if (!current) return "INV-00001";
  const num = parseInt(current.replace("INV-", ""), 10);
  return `INV-${String(num + 1).padStart(5, "0")}`;
}
```

**4. Create `src/components/invoices/status-badge.tsx`:**
```typescript
import { Badge } from "@/components/ui/badge";

const statusConfig: Record<string, { label: string; className: string }> = {
  draft: { label: "Draft", className: "bg-gray-100 text-gray-700 border-gray-200" },
  sent: { label: "Sent", className: "bg-blue-100 text-blue-700 border-blue-200" },
  paid: { label: "Paid", className: "bg-green-100 text-green-700 border-green-200" },
  partial: { label: "Partial", className: "bg-amber-100 text-amber-700 border-amber-200" },
  overdue: { label: "Overdue", className: "bg-red-100 text-red-700 border-red-200" },
  collections: { label: "Collections", className: "bg-red-200 text-red-900 border-red-300" },
};

export function InvoiceStatusBadge({ status }: { status: string }) {
  const config = statusConfig[status] ?? statusConfig.draft;
  return (
    <Badge variant="outline" className={config.className}>
      {config.label}
    </Badge>
  );
}
```
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/billinghub && grep -c "getAgingBucket" src/lib/utils/aging.ts && grep -c "createInvoiceSchema" src/lib/utils/validation.ts && grep -c "getNextInvoiceNumber" src/lib/utils/invoice-number.ts && grep -c "InvoiceStatusBadge" src/components/invoices/status-badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/utils/aging.ts exports `getAgingBucket`, `agingColors`, `agingBadgeColors`, `agingLabels`
    - src/lib/utils/aging.ts returns `null` for non-overdue invoices
    - src/lib/utils/validation.ts exports `createCustomerSchema`, `createInvoiceSchema`, `lineItemSchema`, `recordPaymentSchema`, `createRecurringTemplateSchema`, `collectionNoteSchema`
    - src/lib/utils/validation.ts contains `z.enum(["slip_rental", "fuel", "maintenance", "amenity", "service", "other"])`
    - src/lib/utils/invoice-number.ts exports `getNextInvoiceNumber`
    - src/lib/utils/invoice-number.ts contains `INV-${String(num + 1).padStart(5, "0")}`
    - src/components/invoices/status-badge.tsx exports `InvoiceStatusBadge`
    - src/components/invoices/status-badge.tsx contains className entries for all 6 statuses: draft, sent, paid, partial, overdue, collections
  </acceptance_criteria>
  <done>All shared utilities and components ready for consumption by subsequent plans. Aging buckets match CONTEXT spec (current 0-30, 30day 31-60, 60day 61-90, 90plus). Validation schemas cover all Phase 2 form types. Invoice number generator produces INV-XXXXX format.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (schema and utility types are valid)
- `grep "customerNotes" src/lib/db/schema.ts` returns matches
- All 7 shadcn components installed (dialog, select, tabs, textarea, switch, popover, calendar, progress)
- Sidebar shows Recurring and Collections items
</verification>

<success_criteria>
Schema fully supports all Phase 2 requirements. Shared utilities (aging, validation, invoice number, status badge) are importable by all subsequent plans. Sidebar navigation is complete for Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/02-billing-engine/02-01-SUMMARY.md`
</output>
