---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/db/seed.ts
  - package.json
autonomous: true
requirements: [SEED-01, SEED-02, SEED-04, SEED-05, SEED-06, SEED-07]

must_haves:
  truths:
    - "Seed script creates 25 customer accounts with names, vessel details, slip assignments across docks A-E"
    - "Seed script creates 150+ invoices spanning 6 months with distribution: 60% paid on time, 20% paid late, 15% overdue, 5% in collections"
    - "Invoices contain mixed revenue types: slip_rental, fuel, maintenance, amenity, service line items"
    - "3 customers have partial payment records (payment plans)"
    - "15 long-term slip holders have recurring monthly invoice templates"
    - "All dates are relative to current date so aging buckets remain realistic"
    - "Seed is idempotent: truncates all tables then re-inserts"
  artifacts:
    - path: "src/lib/db/seed.ts"
      provides: "Complete demo data seeding script"
      contains: "subDays"
      min_lines: 200
    - path: "package.json"
      provides: "seed script in scripts section"
      contains: "db:seed"
  key_links:
    - from: "src/lib/db/seed.ts"
      to: "src/lib/db/schema.ts"
      via: "Imports all table definitions for inserts"
      pattern: "import.*schema"
    - from: "src/lib/db/seed.ts"
      to: "src/lib/db/index.ts"
      via: "Uses db instance for queries"
      pattern: "import.*db.*from"
---

<objective>
Create the comprehensive seed script that populates the database with 25 customers, 150+ invoices with realistic aging distribution, mixed revenue line items, payments, recurring templates, and partial payment plans.

Purpose: Demo data makes the application immediately usable and showcase-ready. The aging distribution and mixed revenue types demonstrate BillingHub's value proposition.
Output: Runnable seed script that populates all demo data idempotently.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@BRIEF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive seed script with 25 customers, 150+ invoices, payments, recurring templates, and realistic aging distribution</name>
  <files>
    src/lib/db/seed.ts,
    package.json
  </files>
  <read_first>
    src/lib/db/schema.ts,
    src/lib/db/index.ts,
    BRIEF.md,
    .planning/phases/01-foundation-auth/01-CONTEXT.md
  </read_first>
  <action>
1. Add script to package.json: `"db:seed": "tsx src/lib/db/seed.ts"`

2. Create `src/lib/db/seed.ts` with the following structure:

**Imports:** date-fns (subDays, subMonths, format, addMonths), bcryptjs, db from index, all tables from schema, and drizzle-orm sql/eq helpers.

**Truncation order** (respect foreign keys):
   ```
   collectionNotes -> payments -> lineItems -> invoices -> recurringTemplates -> customers -> users
   ```
   Use `db.delete(table)` for each in this order.

**Demo Users (3):**
   - Hash "demo1234" with bcrypt.hash("demo1234", 10) once, reuse for all 3.
   - manager@billinghub.app / "Harbor Master" / manager
   - billing@billinghub.app / "Sarah Mitchell" / billing_clerk
   - boater@billinghub.app / "James Cooper" / customer
   - Insert with db.insert(users).values([...]).returning() to get IDs.

**Customers (25):**
   Define an array of 25 customer objects with realistic marina data:
   - Names like: "James Cooper" (linked to boater user), "Robert Chen", "Maria Santos", "William Drake", "Elena Vasquez", "Thomas Blackwell", "Sarah Kim", "David Hartwell", "Jennifer Marsh", "Michael O'Brien", "Patricia Wells", "Anthony Romano", "Lisa Chang", "George Palmer", "Diana Cruz", "Richard Stone", "Karen Whitfield", "Steven Park", "Michelle Torres", "Daniel Reed", "Catherine Burke", "Andrew Walsh", "Nicole Foster", "Brian Hayes", "Amanda Sutton"
   - Vessel names: "Sea Breeze", "Windward Spirit", "Blue Horizon", "Pacific Dream", "Silver Wake", "Compass Rose", "Tide Runner", "Harbor Light", "Ocean Pearl", "Sunset Chaser", "Starboard", "Neptune's Call", "Wave Dancer", "Anchor Point", "Bay Runner", "Coral Reef", "Deep Blue", "Emerald Tide", "Fair Wind", "Golden Eagle", "Island Time", "Knot Working", "Lazy Days", "Meridian", "North Star"
   - Vessel types: mix of "Sailboat", "Powerboat", "Catamaran", "Yacht", "Fishing Boat"
   - Vessel lengths: range 22-65 feet
   - Docks: distributed across A, B, C, D, E
   - Slip numbers: "A01" through "E05" format
   - Link James Cooper customer to boater user via userId field.
   - Insert all 25 with db.insert(customers).values([...]).returning().

**Invoice Number Generator:**
   - Track a counter starting at 1.
   - Format: `INV-${String(counter).padStart(5, '0')}` producing INV-00001, INV-00002, etc.

**Invoice Generation (150+ invoices over 6 months):**
   For each of the 15 long-term slip holders, generate 6 monthly invoices (one per month going back 6 months) = 90 invoices. For the remaining 10 customers, generate 2-4 invoices each with varied dates = ~30-40 more invoices. Add 20-30 additional fuel/maintenance/amenity-only invoices across random customers. Total: 150+ invoices.

   Date strategy using date-fns:
   - `const now = new Date()`
   - Monthly invoices: issueDate = subMonths(now, N) for N=0..5, dueDate = 30 days after issueDate
   - All dates formatted as ISO date strings via format(date, "yyyy-MM-dd")

   **Line items per invoice:**
   - Slip rental invoices: primary line item "Monthly Slip Rental - Dock {X} Slip {Y}" with unitPriceCents based on vessel length (40ft+ = 85000 cents/month, 30-39ft = 65000, under 30ft = 45000)
   - Add secondary charges randomly (30% chance each): fuel purchase (category: "fuel", 5000-25000 cents), maintenance (category: "maintenance", 15000-75000 cents), amenity fees (category: "amenity", pump-out 3500 cents, laundry 1500 cents, Wi-Fi premium 4500 cents/month), service charges (category: "service", bottom cleaning 35000 cents, winterization 45000 cents)
   - Each line item: calculate amountCents = quantity * unitPriceCents
   - Invoice subtotalCents = sum of all line item amountCents
   - taxRate = 0.075 (7.5% sales tax)
   - taxAmountCents = Math.round(subtotalCents * taxRate)
   - totalCents = subtotalCents + taxAmountCents

   **Invoice status distribution (apply to the full set):**
   - 60% paid on time: status "paid", create full payment record with paymentDate within 15 days of dueDate
   - 20% paid late: status "paid", create full payment record with paymentDate 5-30 days after dueDate
   - 15% currently overdue: status "overdue", no payment. Distribute aging:
     - ~8% in 30-day bucket (dueDate 31-60 days ago)
     - ~4% in 60-day bucket (dueDate 61-90 days ago)
     - ~3% in 90+ day bucket (dueDate 90+ days ago)
   - 5% in collections: status "collections", no payment, dueDate 90+ days ago. Add 1-2 collection notes per invoice.

   **Payment methods:** Distribute across "check" (40%), "credit_card" (30%), "bank_transfer" (20%), "cash" (10%).

**Partial Payments (3 customers):**
   Select 3 customers from the overdue set. For each, pick one overdue invoice and:
   - Change status to "partial"
   - Create 1-2 payment records where amountCents is 30-50% of totalCents
   - These represent active payment plans.

**Recurring Templates (15 long-term holders):**
   For each of the 15 long-term slip holders, create a recurringTemplate:
   - name: "Monthly Slip Rental - {customerName}"
   - lineItems: JSON array of the slip rental line item template [{category: "slip_rental", description: "Monthly Slip Rental - Dock X Slip Y", quantity: 1, unitPriceCents: XXXX}]
   - frequency: "monthly"
   - nextInvoiceDate: format of first day of next month
   - active: true

**Collection Notes:**
   For each invoice in "collections" status, insert 1-2 collection notes:
   - Note text examples: "Left voicemail regarding outstanding balance", "Spoke with customer, promised payment by end of month", "Sent written notice via certified mail", "Customer requested payment plan - reviewing account"
   - createdBy: manager user ID
   - createdAt: random date between invoice dueDate and now

3. Add console.log statements showing progress:
   - "Seeding users... 3 created"
   - "Seeding customers... 25 created"
   - "Seeding invoices... {N} created with {M} line items"
   - "Seeding payments... {N} created"
   - "Seeding recurring templates... 15 created"
   - "Seeding collection notes... {N} created"
   - "Seed complete!"

4. Run the seed script: `npm run db:seed`
  </action>
  <verify>
    <automated>npm run db:seed 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db/seed.ts contains "subDays" or "subMonths" from "date-fns" (relative dates)
    - src/lib/db/seed.ts contains "bcrypt.hash" and "demo1234"
    - src/lib/db/seed.ts contains "db.delete(" (idempotent truncation)
    - src/lib/db/seed.ts contains "INV-" and "padStart" (invoice number format)
    - src/lib/db/seed.ts contains "slip_rental" and "fuel" and "maintenance" and "amenity" (mixed revenue types)
    - src/lib/db/seed.ts contains "partial" (partial payments)
    - src/lib/db/seed.ts contains "recurringTemplates" (recurring template creation)
    - src/lib/db/seed.ts contains "collections" and "collectionNotes" (collection status + notes)
    - package.json contains "db:seed"
    - npm run db:seed exits 0 and output contains "Seed complete" or "complete"
  </acceptance_criteria>
  <done>Seed script creates 25 customers, 150+ invoices with correct aging distribution (60/20/15/5%), mixed revenue line items, payments, 15 recurring templates, 3 partial payment plans, and collection notes. Script is idempotent and uses relative dates.</done>
</task>

</tasks>

<verification>
1. `npm run db:seed` completes without errors
2. Database contains 3 users, 25 customers, 150+ invoices, corresponding line items and payments
3. Invoice aging distribution: ~60% paid, ~20% paid late, ~15% overdue, ~5% collections
4. At least 5 different charge categories present in line items
5. 15 recurring templates exist with monthly frequency
6. 3 customers have partial payment records
7. Running seed script again produces same result (idempotent)
</verification>

<success_criteria>
- Database populated with comprehensive, realistic demo data
- Invoice aging distribution matches spec (60/20/15/5%)
- Mixed revenue types across invoices (slip_rental, fuel, maintenance, amenity, service)
- Relative dates ensure aging stays realistic regardless of run date
- Seed script is idempotent (safe to run multiple times)
- 15 recurring templates for long-term slip holders
- 3 customers with active partial payment plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
