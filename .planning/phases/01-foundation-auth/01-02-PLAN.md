---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/dashboard/page.tsx
  - src/app/(admin)/page.tsx
  - src/app/(portal)/layout.tsx
  - src/app/(portal)/page.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/top-bar.tsx
  - src/components/layout/breadcrumbs.tsx
autonomous: true
requirements: [AUTH-04, AUTH-05, AUTH-06, UI-09, UI-10, UI-11, UI-12]

must_haves:
  truths:
    - "Login page shows split layout with hero-marina.png on right panel, gradient fade, wave pattern at bottom-left at 5-8% opacity"
    - "Manager sees admin sidebar with full navigation (Dashboard, Invoices, Payments, Customers, Reports, Settings)"
    - "Billing clerk sees admin sidebar without Settings"
    - "Customer sees portal layout with top-bar (logo, name, logout) and no sidebar"
    - "Sidebar collapses to 72px icons at 768px, becomes overlay at 640px"
    - "Breadcrumbs display dynamically from route segments"
    - "Logo.svg renders at 36px with BillingHub wordmark in sidebar"
    - "Demo credentials card visible below login form"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Styled split-screen login page"
      contains: "hero-marina.png"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Admin sidebar navigation"
      contains: "sidebar"
    - path: "src/components/layout/top-bar.tsx"
      provides: "Portal top bar"
      contains: "top-bar"
    - path: "src/app/(admin)/layout.tsx"
      provides: "Admin shell with sidebar"
    - path: "src/app/(portal)/layout.tsx"
      provides: "Portal shell with top-bar"
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "src/lib/auth/session.ts"
      via: "getSession to determine role for nav filtering"
      pattern: "getSession"
    - from: "src/app/(admin)/layout.tsx"
      to: "src/components/layout/sidebar.tsx"
      via: "Import and render sidebar"
      pattern: "import.*sidebar"
---

<objective>
Build the three application shell layouts (admin with sidebar, portal with top-bar, auth with split-screen) and style the login page per DESIGN-SPEC.md with hero image, wave pattern, and demo credentials card.

Purpose: Users see a polished, role-appropriate application shell that matches the design specification. This is the visual foundation all feature pages render within.
Output: Three route group layouts, styled login page, sidebar navigation, portal top-bar, breadcrumbs.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@DESIGN-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build admin sidebar, portal top-bar, breadcrumbs components and all three route group layouts</name>
  <files>
    src/components/layout/sidebar.tsx,
    src/components/layout/top-bar.tsx,
    src/components/layout/breadcrumbs.tsx,
    src/app/(admin)/layout.tsx,
    src/app/(admin)/dashboard/page.tsx,
    src/app/(admin)/page.tsx,
    src/app/(portal)/layout.tsx,
    src/app/(portal)/page.tsx
  </files>
  <read_first>
    src/lib/auth/session.ts,
    src/lib/auth/actions.ts,
    src/app/globals.css,
    src/app/layout.tsx,
    DESIGN-SPEC.md
  </read_first>
  <action>
1. Create `src/components/layout/sidebar.tsx`:
   - Server component that calls getSession() to get user role and name.
   - Sidebar background: bg-sidebar-bg (#0C2D48 light / #070F1A dark) with width 260px expanded, 72px collapsed.
   - Top: logo.svg rendered at 36px width via next/image or img tag, with "BillingHub" wordmark in DM Serif Display 400, 18px, white. In collapsed mode show icon only.
   - Navigation items using Lucide icons (20px, stroke-width 1.5):
     - Main group: Dashboard (LayoutDashboard), Invoices (FileText), Payments (DollarSign), Customers (Users)
     - Reports group (divider above): Reports (BarChart3)
     - Settings group (divider above, MANAGER ONLY): Settings (Settings)
   - Active item styling: bg-[rgba(27,107,147,0.25)], left border 3px solid harbor-gold (#E8AA42), border-radius 0 8px 8px 0
   - Inactive item text color: #A8C4D8, active: white
   - Hover: bg-[#0F3A5C]
   - Nav item font: Inter 500 14px
   - Divider between groups: 1px solid rgba(168, 196, 216, 0.15)
   - Bottom section: user avatar circle (32px, initials), name, role badge, logout button calling logout server action, dark mode toggle using next-themes useTheme
   - Collapse behavior: Use client-side state. At 768px breakpoint collapse to icon-only (72px). At 640px become sheet/overlay (use shadcn Sheet component). Store collapse preference in localStorage.
   - Hide Settings nav item if role !== "manager" (AUTH-04/AUTH-05 differentiation).

2. Create `src/components/layout/top-bar.tsx`:
   - Portal navigation bar for customers.
   - Full width, white/card background, shadow below.
   - Left: logo.svg at 36px + "BillingHub" wordmark.
   - Right: Customer name, dark mode toggle, logout button (calling logout action).
   - No sidebar links -- this is a minimal top bar per DESIGN-SPEC.md Section 7 "Customer Portal."
   - Height: ~64px, padding 16px 24px.

3. Create `src/components/layout/breadcrumbs.tsx`:
   - Uses `usePathname()` from next/navigation to parse route segments.
   - Renders breadcrumb trail: Inter 400 13px, color #6B7A8D, separator "/", current page in #0C2D48 font-weight 500.
   - Capitalize and prettify segment names (e.g., "dashboard" -> "Dashboard", "invoices" -> "Invoices").

4. Create `src/app/(admin)/layout.tsx`:
   - Server component. Calls getSession(). If no session, redirect to /login (safety net -- middleware handles primary).
   - Renders Sidebar + content area.
   - Content area: padding 32px, max-width 1400px centered.
   - Include Breadcrumbs component at top of content area.

5. Create `src/app/(admin)/page.tsx`:
   - Redirect to /dashboard using next/navigation redirect().

6. Create `src/app/(admin)/dashboard/page.tsx`:
   - Placeholder page with heading "Revenue Dashboard" in DM Serif Display (h1).
   - Subtitle "Welcome back" with the user name from getSession().
   - This is a placeholder -- Phase 3 builds the real dashboard.
   - Use dashboard-header.png as background on a header card with rgba(12, 45, 72, 0.85) overlay and white text.

7. Create `src/app/(portal)/layout.tsx`:
   - Server component. Calls getSession(). If no session, redirect to /login.
   - Renders TopBar + content area.
   - Content area: padding 32px, max-width 1200px centered.

8. Create `src/app/(portal)/page.tsx`:
   - Placeholder page showing "Welcome, {name}" with account summary card placeholder.
   - Body text at 15px per DESIGN-SPEC.md portal spec.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/sidebar.tsx contains "logo.svg" and "BillingHub" and "Settings" and "getSession" and "260" and "72" and "#E8AA42" and "logout"
    - src/components/layout/top-bar.tsx contains "logo.svg" and "BillingHub" and "logout"
    - src/components/layout/breadcrumbs.tsx contains "usePathname" and "breadcrumb"
    - src/app/(admin)/layout.tsx contains "Sidebar" and "getSession" and "1400"
    - src/app/(admin)/dashboard/page.tsx contains "dashboard-header.png" and "Revenue Dashboard"
    - src/app/(portal)/layout.tsx contains "TopBar" and "getSession"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Admin layout renders sidebar with role-filtered navigation. Portal layout renders top-bar. Dashboard placeholder shows header with background image. Sidebar collapses at 768px and overlays at 640px.</done>
</task>

<task type="auto">
  <name>Task 2: Style the login page with split layout, hero image, wave pattern, and demo credentials per DESIGN-SPEC.md</name>
  <files>
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/layout.tsx
  </files>
  <read_first>
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/layout.tsx,
    src/app/globals.css,
    src/lib/auth/actions.ts,
    DESIGN-SPEC.md
  </read_first>
  <action>
1. Update `src/app/(auth)/layout.tsx`:
   - Minimal layout wrapper. Full viewport height, no sidebar or top-bar.
   - If user is already logged in (check getSession()), redirect to appropriate page based on role.

2. Rewrite `src/app/(auth)/login/page.tsx` with DESIGN-SPEC.md Section 7 "Login / Landing" styling:
   - "use client" component.
   - Split screen layout: left 55% content, right 45% hero image.
   - LEFT SIDE:
     - Background: bg-background (#F6F8FB light).
     - Center vertically: logo.svg at 48px + "BillingHub" wordmark in DM Serif Display 700 (h1 size).
     - Tagline below: "Marina billing, unified." in Inter 400, 16px, color #6B7A8D.
     - Login form centered with generous whitespace (max-width ~400px):
       - Email input with label "Email", placeholder "you@marina.com"
       - Password input with label "Password", type="password"
       - Submit button: full width, bg-btn-gradient class (linear-gradient(135deg, #1B6B93 0%, #0C2D48 100%)), white text, Inter 500, 14px, border-radius 8px, padding 10px 20px
       - Error message displayed in danger color (#DC3545) if login returns error
     - Demo credentials card below form: subtle card (bg-muted, rounded-lg, p-4) showing:
       - "Demo Accounts" heading in Inter 600 12px uppercase
       - manager@billinghub.app / demo1234 -- Manager
       - billing@billinghub.app / demo1234 -- Billing Clerk
       - boater@billinghub.app / demo1234 -- Customer
     - Wave pattern: wave-pattern.svg positioned at bottom of left panel at 5-8% opacity as background-image with repeat-x, positioned at bottom.
   - RIGHT SIDE:
     - hero-marina.png fills panel edge-to-edge with background-size: cover, background-position: center.
     - Gradient overlay from left: linear-gradient to right, from #F6F8FB (opacity 100%) to transparent over ~60px, creating seamless blend with left panel.
   - Responsive: Below 768px, hide the right panel entirely, left panel becomes full-width.
   - Form submission: Use useActionState (React 19) or form action with the login server action. Show loading state on submit button.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(auth)/login/page.tsx contains "hero-marina.png" and "wave-pattern.svg" and "Marina billing, unified" and "Demo Accounts" and "manager@billinghub.app" and "billing@billinghub.app" and "boater@billinghub.app" and "55%" and "45%"
    - src/app/(auth)/layout.tsx contains "getSession" or renders children without sidebar
    - npm run build exits 0
  </acceptance_criteria>
  <done>Login page renders the DESIGN-SPEC.md split layout with hero image on right, gradient blend, wave pattern at bottom-left, centered login form, and demo credentials card. Responsive at 768px hides hero panel.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Login page shows split layout with hero image, wave pattern, demo credentials
3. Manager login shows sidebar with Dashboard, Invoices, Payments, Customers, Reports, Settings
4. Clerk login shows sidebar without Settings
5. Customer login shows portal top-bar without sidebar
6. Sidebar collapses at 768px breakpoint
7. Breadcrumbs display on admin pages
8. Logo and wordmark visible in sidebar and portal top-bar
</verification>

<success_criteria>
- Three route group layouts render correctly ((auth), (admin), (portal))
- Login page matches DESIGN-SPEC.md Section 7 "Login / Landing" visual spec
- Sidebar shows role-appropriate navigation items
- Portal uses top-bar navigation without sidebar
- Responsive breakpoints work at 1400/1200/768/640px
- All pre-generated assets (logo.svg, hero-marina.png, wave-pattern.svg, dashboard-header.png) integrated
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
